import * as React from 'react';


export const EmailTemplate = ({
                                  email,
                              }) => (
    <div>
        <h1>Welcome, {email}!</h1>
    </div>
);